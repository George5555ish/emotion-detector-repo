import {makeStyles} from '@material-ui/core';


export default makeStyles((theme) => ({

  mainDiv: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  }

}));